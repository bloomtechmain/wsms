# Railway Environment Variables for wsms-client
# Copy these to Railway Dashboard → wsms-client → Variables

# API URL (Railway auto-references the server service)
VITE_API_URL=${{wsms-server.RAILWAY_PUBLIC_DOMAIN}}/api

# Port (default for client)
PORT=5173

# NOTE: In Railway, use the Variable reference syntax above.
# Railway will automatically resolve ${{ServiceName.VARIABLE}} references.
# 
# If auto-reference doesn't work, manually set:
# VITE_API_URL=https://your-actual-server-url.railway.app/api

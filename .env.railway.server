# Railway Environment Variables for wsms-server
# Copy these to Railway Dashboard → wsms-server → Variables

# Database Connection (Railway auto-links this)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT Secret for authentication (already generated securely)
JWT_SECRET=7f8a9c2b4e6d1f3a8b5c9e0d2f4a6b8c1e3d5f7a9b0c2d4e6f8a0b1c3d5e7f9a

# Server Port (Railway standard)
PORT=8080

# Environment
NODE_ENV=production

# Client URL (Railway auto-references the client service)
CLIENT_URL=${{wsms-client.RAILWAY_PUBLIC_DOMAIN}}

# NOTE: In Railway, use the Variable reference syntax above.
# Railway will automatically resolve ${{ServiceName.VARIABLE}} references.
